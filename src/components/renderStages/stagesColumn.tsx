import { ActionChainStage } from "../../lib/mongo"

export const StagesColumn = (props: { stages: ActionChainStage[] }) => {
    return (
        <div>
            stage column
        </div>
    );
};